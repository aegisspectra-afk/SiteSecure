SITE SECURE Lottie source library.

Do not import these files from application screens.

Production copies of approved animations live in:
  apps/web/src/assets/lottie/

Usage rules, placement map, and UX purpose for each animation:
  Docs/ux/LOTTIE-REGISTRY.md

Do not add a production animation without a written UX purpose first.

Cloud / backup motion is not production until a real sync probe exists.
Network Connecting is production only for a real browser-offline state.
